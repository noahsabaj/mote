/**
 * The model's name, as it calls itself. Mirrors NAME in mote/serve/identity.py — the
 * `name` field of /api/model is the loaded checkpoint's path, which is a different thing.
 */
export const MODEL_NAME = 'Mote';
